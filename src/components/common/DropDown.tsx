import React from "react";
import styled from "@emotion/styled";

interface PropsType {
  title: string;
}

function DropDown({ title }: PropsType) {
  return (
    <>
      <></>
    </>
  );
}

export default React.memo(DropDown);
